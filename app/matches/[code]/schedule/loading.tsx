import { OrganizerFormLoading } from "@/components/feedback/organizer-form-loading";

export default function ScheduleMatchLoading() {
  return (
    <OrganizerFormLoading fields={3} label="Loading the match schedule form." />
  );
}
